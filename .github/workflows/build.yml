name: Build Flutter APK

on:
  push:
    branches: [ main ]
    tags:
      - 'v*'
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    name: Build Flutter APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          cache: true

      - name: Flutter Doctor
        run: flutter doctor -v

      - name: Get Dependencies
        run: flutter pub get

      - name: Analyze
        run: flutter analyze

      - name: Test
        run: flutter test

      - name: Decode release keystore
        if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
        env:
          KEYSTORE_BASE64: ${{ secrets.GULSHAN_TUBE_KEYSTORE_BASE64 }}
          GULSHAN_TUBE_STORE_PASSWORD: ${{ secrets.GULSHAN_TUBE_STORE_PASSWORD }}
        run: |
          if [ -z "$KEYSTORE_BASE64" ]; then
            echo "::error::GULSHAN_TUBE_KEYSTORE_BASE64 secret is not set."
            echo "Add it with: base64 -w0 gulshantube-release.jks"
            exit 1
          fi
          echo "$KEYSTORE_BASE64" | base64 -d > android/app/gulshantube-release.jks
          # Confirm the keystore decoded and the password opens it, without
          # printing any secret material into the log.
          keytool -list -keystore android/app/gulshantube-release.jks \
            -storepass "$GULSHAN_TUBE_STORE_PASSWORD" > /dev/null
          echo "Keystore decoded and verified."

      - name: Build signed release APK
        if: github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository
        env:
          # Gradle reads these in signingConfigs.release. No fallbacks: a
          # missing secret must fail the build rather than silently produce
          # an unsigned or debug-signed APK.
          GULSHAN_TUBE_STORE_PASSWORD: ${{ secrets.GULSHAN_TUBE_STORE_PASSWORD }}
          GULSHAN_TUBE_KEY_ALIAS: ${{ secrets.GULSHAN_TUBE_KEY_ALIAS }}
          GULSHAN_TUBE_KEY_PASSWORD: ${{ secrets.GULSHAN_TUBE_KEY_PASSWORD }}
        run: flutter build apk --release

      - name: Build debug APK for fork pull requests
        if: github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name != github.repository
        run: flutter build apk --debug

      - name: Collect APK
        run: |
          mkdir -p dist
          if [ -f build/app/outputs/flutter-apk/app-release.apk ]; then
            cp build/app/outputs/flutter-apk/app-release.apk dist/GULSHAN TUBE-release.apk
          else
            cp build/app/outputs/flutter-apk/app-debug.apk dist/GULSHAN TUBE-debug.apk
          fi
          ls -lah dist/

      - name: Remove decoded keystore
        if: always()
        run: rm -f android/app/gulshantube-release.jks

      - name: Upload APK Artifact
        uses: actions/upload-artifact@v4
        with:
          name: GULSHAN TUBE-Flutter-APK
          path: dist/GULSHAN TUBE-release.apk
          retention-days: 30

  release:
    name: Create Release
    needs: build
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    # Default GITHUB_TOKEN is read-only in this repo, which made
    # action-gh-release fail with "Resource not accessible by integration"
    # (403). Creating a release and uploading the APK needs contents: write.
    permissions:
      contents: write

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          # Need tags to work out the previous release for the compare link.
          fetch-depth: 0

      - name: Download APK
        uses: actions/download-artifact@v4
        with:
          name: GULSHAN TUBE-Flutter-APK
          path: release/

      - name: Build release notes from CHANGELOG
        id: notes
        run: |
          TAG="${GITHUB_REF_NAME}"
          # Previous tag by version order, so the compare link is meaningful.
          PREV=$(git tag --sort=-v:refname | grep -A1 -x "$TAG" | tail -1)
          if [ "$PREV" = "$TAG" ]; then PREV=""; fi
          echo "Building notes for $TAG (previous: ${PREV:-none})"
          if python3 scripts/release_notes.py "$TAG" \
               ${PREV:+--previous "$PREV"} > release-notes.md; then
            echo "generated=true" >> "$GITHUB_OUTPUT"
            echo "----- release notes -----"
            cat release-notes.md
          else
            # No CHANGELOG entry: fall back to autogenerated notes rather
            # than publishing an empty release body.
            echo "generated=false" >> "$GITHUB_OUTPUT"
            echo "::warning::No CHANGELOG entry for $TAG; using autogenerated notes."
          fi

      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          files: release/*.apk
          body_path: ${{ steps.notes.outputs.generated == 'true' && 'release-notes.md' || '' }}
          generate_release_notes: ${{ steps.notes.outputs.generated != 'true' }}
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
